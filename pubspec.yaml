name: aoo_clientes
description: "Telecell Mobile — Gestor de Ordens de Serviço & Inteligência de Estoque"
publish_to: 'none'

version: 1.1.0+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.15
  path_provider: ^2.1.2
  path: any
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0

  # ── Módulo Agente Analista ────────────────────────────────────────────────
  flutter_gemma: ^0.13.1
  flutter_gemma_litertlm: ^0.13.1
  google_generative_ai: ^0.4.7
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  drift_dev: ^2.14.0
  build_runner: ^2.4.7
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
